using Polylabel
using Karmana, CairoMakie

# Here, we create a map with all states labelled at their "poles of inaccessibility", computed by `Polylabel.jl`.

# This is a very simple example, but it shows how to use `Karmana.jl` to create a map with labels.

